@{
    ViewBag.Title = "HienThiNhomView";
    Layout = "~/Views/Shared/_LayoutPageConLai.cshtml";
}

@using Web.Models;
@model List<Nhom>
<div class="container" style="margin-top:20px">
<div class="nav bg-ligth ">
    <div class="card-group ">
      <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-2 " style="width:200px; height: 200px">
                    <div class="card ">     
                         <img class="card-img-top" style="width:150px;height:100px" src="~/Content/image/@item.hinhanhdaidien" />                 
                        <div class="card-body bg-light">   
                            <p class="card-title ">
                                <a class="nav-link" href="@Url.Action("HienThiLoai",new { ms= @item.maNhom })">@item.tenNhom</a>
                            </p>                                               
                        </div>
                    </div>
                </div>         
            }
   </div>
</div>
   </div>
</div>
@Html.Action("SanPhamAll")

